# Issues

#### Lists the n issues from a Github repo

## Usage

1) Clone or download to your Unix-based system
2) Run the escript like so `./issues <github username> <project> [number of issues (default 4)]`
3) ...
4) Profit

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed
by adding `issues` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [
    {:issues, "~> 0.1.0"}
  ]
end
```
